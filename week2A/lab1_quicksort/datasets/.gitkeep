# Datasets will be generated by `make datasets`.
# Default size comes from Makefile variable N (default: 100000).
#
# Examples:
#   make datasets           # generates *_100000.txt by default
#   make N=50000 datasets   # generates *_50000.txt
#
# Manual equivalents:
#   Random: shuf -i 1-N -n N > random_N.txt
#   Sorted: seq 1 N > sorted_N.txt
#   Reverse: seq N -1 1 > reverse_N.txt
#   Nearly sorted: seq 1 N | awk -v N=N 'BEGIN{srand()} {if(rand()<0.1) print int(rand()*N)+1; else print}' > nearly_sorted_N.txt
